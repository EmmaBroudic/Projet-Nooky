/*import { writable } from "svelte/store";

export interface InputsSignUp {
    id: number;
    username: string;
    email: string;
    password: string;
    firstname: string;
    lastname: string;
    road: string;
    city: string;
    zipcode: number;
}

export const keepInputsSignUp = writable<InputsSignUp[]>([]);

export interface InputsSignIn {
    id: number;
    email: string;
    password: string;
}

export const keepInputsSignIn = writable<InputsSignIn[]>([]);

interface InputsProduct {
    id: number;
    name: string;
    description: string;
    picture: string;
}

export const keepInputsProduct = writable<InputsProduct[]>([]);*/